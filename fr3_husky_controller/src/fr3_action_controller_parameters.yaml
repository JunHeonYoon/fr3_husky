fr3_action_controller:
  # FR3
  robot_name: {
    type: string_array,
    default_value: ["left", "right"],
    description: "Robot name want to control (left/right)"
  }
  hand: {
    type: bool,
    default_value: true,
    description: "Use franka hand?",
  }
  mobile_base: {
    type: bool,
    default_value: false,
    description: "Use Husky as base?",
  }
  manipulator_joints: {
    type: string_array,
    default_value: [],
    description: "Joint names of the manipulator system",
    read_only: true,
    validation: {
      unique<>: null,
      size_gt<>: [0],
    }
  }
  manipulator_command_interface: {
    type: string,
    default_value: "effort",
    description: "Command interface provided by the hardware interface for manipulator joints",
    read_only: true,
    validation: {
      one_of<>: [["position", "velocity", "effort"]],
    }
  }
  manipulator_state_interfaces: {
    type: string_array,
    default_value: ["position", "velocity", "effort"],
    description: "State interfaces provided by the hardware for manipulator joints.",
    read_only: true,
    validation: {
      unique<>: null,
      size_gt<>: [0],
      subset_of<>: [["position", "velocity", "effort",]],
    }
  }
  # #  Dyros Robot Controller
  # dyros_robot_controller:
  #   manipulator_joint_gains: 
  #     use_mass_matrix: {
  #       type: bool,
  #       default_value: false,
  #       description: "When set to true, use joint space mass matrix for PD control (size must be 7*num_robot)"
  #     }
  #     kp: {
  #       type: double_array,
  #       default_value: [],
  #       description: "Manipulator joint position gains for PD control (size must be 7*num_robot)"
  #     }
  #     kv: {
  #       type: double_array,
  #       default_value: [],
  #       description: "Manipulator joint velocity gains for PD control (size must be 7*num_robot)"
  #     }
  #   task_gains:
  #     link_names: {
  #       type: string_array,
  #       default_value: [],
  #       description: "Link names for setting PD control"
  #     }
  #     kp: {
  #       type: double_array,
  #       default_value: [],
  #       description: "Task space position gains for PD control (size must be 6 * link_names)"
  #     }
  #     kv: {
  #       type: double_array,
  #       default_value: [],
  #       description: "Task space velocity gains for PD control (size must be 6 * link_names)"
  #     }
  #   QPIK_weight:
  #     tracking:
  #       link_names: {
  #         type: string_array,
  #         default_value: [],
  #         description: "Link names for setting QPIK weights"
  #       }
  #       weights: {
  #         type: double_array,
  #         default_value: [],
  #         description: "Weight for track given task space velocity (size must be 6 * link_names)"
  #       }
  #     joint:
  #       velocity: 
  #         manipulator: {
  #           type: double_array,
  #           default_value: [],
  #           description: "Weight for damping manipulator joint velocity (size must be 7 * num_robot)"
  #         }
  #   QPID_weight:
  #     tracking:
  #       link_names: {
  #         type: string_array,
  #         default_value: [],
  #         description: "Link names for setting QPID weights"
  #       }
  #       weights: {
  #         type: double_array,
  #         default_value: [],
  #         description: "Weight for track given task space acceleration (size must be 6 * link_names)"
  #       }
  #     joint:
  #       velocity: 
  #         manipulator: {
  #           type: double_array,
  #           default_value: [],
  #           description: "Weight for damping manipulator joint velocity (size must be 7 * num_robot)"
  #         }
  #       acceleration: 
  #         manipulator: {
  #           type: double_array,
  #           default_value: [],
  #           description: "Weight for damping manipulator joint acceleration (size must be 7 * num_robot)"
  #         }
